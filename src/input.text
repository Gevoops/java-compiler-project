/* find largest */
a, b: float;
c, d: int;
e , f, g: float;
h: int;
{
    input(a);
    a = -4 + a;
    while (a > 0) {
        a = a - 1;
        if(a == 3) {
                   break;
                }
        else {

            }
        output(a);
        }

    output(-4);
}
