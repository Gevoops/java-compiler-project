/* gev zoran */
a, b: float;
c, d: int;
e , f, g: float;
h: int;
{
    input(c);
    switch(c) {
      case 1:
        output(1);
        break;
      case 2:
        while (c + 4 > 0) {
            c = c - 1;
            output(32);
        }
      case 3:
        output(3);
      case 4:
        output(4);
      case 5:
        output(5);
      default: {
        if(c / 2.0 > c / 2){
            output(1);
        } else {
            output(2);
        }
      }
    }
}
